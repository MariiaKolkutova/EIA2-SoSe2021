"use strict";
/*namespace farmalt {

    let grassTotal: number = 100;
    let seedsTotal: number = 120;
    let cornTotal: number = 60;
    let meatTotal: number = 50;
    let carrotsTotal: number = 70;

    console.log(grassTotal);

    class Animal {
        animal: string;
        sound: string;
        name: string;
        foodType: string;
        foodNumber: number;
      


        constructor(_animal: string, _sound: string, _name: string, _foodType: string, _foodNumber: number) {
            this.animal = _animal;
            this.sound = _sound;
            this.name = _name;
            this.foodType = _foodType;
            this.foodNumber = _foodNumber;

            this.eat(_foodNumber);
        }

        eat(_foodTotal: number): void {
            grassTotal -= _foodTotal;
            console.log(grassTotal);
        }

    }

    let animalCow: Animal = new Animal("Cow", "Moo", "Sarah", "grass", 50);
   

    let animalChicken: Animal = new Animal("Chicken", "gack", "Robert", "seeds", 3);

    let animalPigs: Animal = new Animal("Pig", "oink", "Schweini", "corn", 15);

    let animalDog: Animal = new Animal("Dog", "bark", "Fleck", "meat", 5);

    let animelHorse: Animal = new Animal("Horse", "ohoho", "Spirit", "carrots", 10);



    console.log(animalChicken);
}*/ 
//# sourceMappingURL=farm.js.map